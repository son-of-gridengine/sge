HOSTNAMEUTILS(1)           Grid Engine User Commands          HOSTNAMEUTILS(1)



NNAAMMEE
       gethostname,  gethostbyname,  gethostbyaddr,  getservbyname - get local
       host information

SSYYNNTTAAXX
       ggeetthhoossttnnaammee [[--hheellpp||--nnaammee||--aannaammee||--aallll]]

       ggeetthhoossttbbyynnaammee [[--hheellpp||--nnaammee||--aannaammee||--aallll]] <<nnaammee>>

       ggeetthhoossttbbyyaaddddrr [[--hheellpp||--nnaammee||--aannaammee||--aallll]] <<iipp>>

       ggeettsseerrvvbbyynnaammee [[--hheellpp||--nnuummbbeerr]] <<sseerrvviiccee>>

DDEESSCCRRIIPPTTIIOONN
       _g_e_t_h_o_s_t_n_a_m_e and _g_e_t_h_o_s_t_b_y_n_a_m_e are used to get the local  resolved  host
       name.   _g_e_t_h_o_s_t_b_y_a_d_d_r  is  used  to  get the hostname of a specified IP
       address (dotted decimal notation).  _g_e_t_s_e_r_v_b_y_n_a_m_e can be  used  to  get
       the configured port number of a service (e.g. from /etc/services).

       The  hostname  utils are primarily used by the Grid Engine installation
       scripts.  _g_e_t_h_o_s_t_n_a_m_e , _g_e_t_h_o_s_t_b_y_n_a_m_e and _g_e_t_h_o_s_t_b_y_a_d_d_r called  without
       any  option will print out the hostname, all specified aliases, and the
       IP address of the locally  resolved  hostname.   Calling  _g_e_t_s_e_r_v_b_y_n_a_m_e
       without any option will print out the full service entry.

OOPPTTIIOONNSS
   --hheellpp
       Prints a list of all options.

   --nnaammee
       This option only reports the primary name of the host.

   --aannaammee
       If this option is set, the Grid Engine host alias file is used for host
       name resolving.  It  is  necessary  to  set  the  environment  variable
       SGE_ROOT and, if more than one cell is defined, also SGE_CELL.

       This option will print out the Grid Engine host name.

   --aallll
       By  using  the  --aallll  option  all  available  host  information will be
       printed.  This information includes the host name, the Grid Engine host
       name, all host aliases, and the IP address of the host.

   --nnuummbbeerr
       This  option  will  print  out the port number of the specified service
       name.

   <<nnaammee>>
       The host name for which the information is requested.

   <<iipp>>
       The IP address (dotted decimal notation) for which the  information  is
       requested.

   <<sseerrvviiccee>>
       The  service  name  for  which  the information is requested (e.g. ftp,
       sge_qmaster or sge_execd).

EEXXAAMMPPLLEESS
       The following example shows how to get the port number of the FTP  ser-
       vice:

              >getservbyname -number ftp
              21

       The  next  example  shows  the output of gethostname -all when the host
       alias file contains this line:

       gridmaster extern_name extern_name.mydomain

       The local host resolving must also provide the alias name "gridmaster".
       Each  Grid  Engine  host  that wants to use the cluster must be able to
       resolve the host name "gridmaster".

       To setup an alias name, edit your /etc/hosts file or  modify  your  NIS
       setup to provide the alias for the NIS clients.

       The  host  alias  file  must  be  readable from each host (use e.g. NFS
       shared file location).

              >gethostname -all
              Hostname: extern_name.mydomain
              SGE name: gridmaster
              Aliases:  loghost gridmaster
              Host Address(es): 192.168.143.99

EENNVVIIRROONNMMEENNTTAALL VVAARRIIAABBLLEESS
       SSGGEE__RROOOOTT       Specifies the location of the Grid Engine standard  con-
                      figuration files.

       SSGGEE__CCEELLLL       If set, specifies the default Grid Engine cell.

SSEEEE AALLSSOO
       _s_g_e___i_n_t_r_o(1), _h_o_s_t___a_l_i_a_s_e_s(5),

CCOOPPYYRRIIGGHHTT
       See _s_g_e___i_n_t_r_o(1) for a full statement of rights and permissions.



SGE 8.0.0                $Date: 2009-03-12 16:06:25 $         HOSTNAMEUTILS(1)
